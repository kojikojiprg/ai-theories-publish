---
title: "混合精度学習(Mixed Precision Training)(実装・実験編 4/4)"
---

この記事は後編(実装・実験編 4/4)です。前の内容は [こちら](https://zenn.dev/kojikojiprg/books/ai-theories-roadmap/viewer/011_mixed_precision_training-practice-3)。

### 6.14 実験 H: 学習スループット

**反復回数の宣言**: `NUM_ITERATIONS_H`(計測反復数)・`WARMUP_ITERATIONS_H`
(ウォームアップ反復数、5.3 節で水準ごとに宣言済み)を使う。モデル・optimizer は
条件ごとに **1 回だけ構築** し、以降の反復では同じインスタンスを使い回す
(反復のたびに再構築しない)。ウォームアップ後、条件1・条件4 を 1 反復ずつ
交互に計測する。

**前提条件**:

- P5-a(ウォームアップの担保): 先頭と末尾の数反復の平均の差が、全体平均の
  10% 以内であること。
- P5-b(判定精度の担保): 平均の標準誤差が平均の 10% 以下であること。



```python
class _ConditionRunner:
    # モデル・optimizer を条件ごとに1回だけ構築し、以降は使い回して1ステップずつ計測する。
    def __init__(self, precision_level: str, seed: int = 0):
        self.model = build_model(seed).to(device)
        self.optimizer = AdamW(self.model.parameters(), lr=BASE_LEARNING_RATE, weight_decay=WEIGHT_DECAY)
        self.autocast_dtype = torch.float16 if precision_level == "fp16_dynamic" else None
        self.loss_scaler = (
            DynamicLossScaler(**DYNAMIC_SCALER_KWARGS) if precision_level == "fp16_dynamic" else None
        )
        self.generator = torch.Generator(device="cpu")
        self.generator.manual_seed(seed)

    def step(self) -> float:
        inputs, targets = get_random_batch(train_ids, BATCH_SIZE, SEQUENCE_LENGTH, self.generator)
        inputs, targets = inputs.to(device), targets.to(device)
        if device.type == "cuda":
            torch.cuda.synchronize()
        elif device.type == "mps":
            torch.mps.synchronize()
        _t0 = time.perf_counter()
        if self.autocast_dtype is not None:
            with torch.autocast(device_type=device.type, dtype=self.autocast_dtype):
                logits = self.model(inputs)
                loss = F.cross_entropy(logits.reshape(-1, logits.size(-1)), targets.reshape(-1))
        else:
            logits = self.model(inputs)
            loss = F.cross_entropy(logits.reshape(-1, logits.size(-1)), targets.reshape(-1))
        scaled_loss = self.loss_scaler.scale_loss(loss) if self.loss_scaler is not None else loss
        self.optimizer.zero_grad()
        scaled_loss.backward()
        if self.loss_scaler is not None:
            found_inf = self.loss_scaler.unscale_gradients(self.model.parameters())
            if not found_inf:
                self.optimizer.step()
            self.loss_scaler.update(found_inf)
        else:
            self.optimizer.step()
        if device.type == "cuda":
            torch.cuda.synchronize()
        elif device.type == "mps":
            torch.mps.synchronize()
        return time.perf_counter() - _t0


_runner1 = _ConditionRunner("fp32")
_runner4 = _ConditionRunner("fp16_dynamic")

for _ in range(WARMUP_ITERATIONS_H):
    _runner1.step()
    _runner4.step()

_t1_list, _t4_list = [], []
for _ in range(NUM_ITERATIONS_H):
    _t1_list.append(_runner1.step())
    _t4_list.append(_runner4.step())
assert len(_t1_list) == LEVELS[CURRENT_LEVEL_NAME]["NUM_ITERATIONS_H"]
assert len(_t4_list) == LEVELS[CURRENT_LEVEL_NAME]["NUM_ITERATIONS_H"]
_t1_arr, _t4_arr = np.array(_t1_list), np.array(_t4_list)

print(f"WARMUP_ITERATIONS_H={WARMUP_ITERATIONS_H}, NUM_ITERATIONS_H={NUM_ITERATIONS_H}")

# 診断量: モデルの各次元が8の倍数か(T4のTensor Core利用条件)。
print(f"次元が8の倍数か: D_MODEL({D_MODEL})={D_MODEL % 8 == 0}, D_FF({D_FF})={D_FF % 8 == 0}, "
      f"SWIGLU_D_FF({SWIGLU_D_FF})={SWIGLU_D_FF % 8 == 0}")

# 前提条件 P5-a: 先頭と末尾の数反復の平均の差が全体平均の10%以内であること。
_P5A_DRIFT_RATIO = 0.10
_half = max(1, NUM_ITERATIONS_H // 2)
_drift_1 = abs(_t1_arr[:_half].mean() - _t1_arr[_half:].mean())
_drift_4 = abs(_t4_arr[:_half].mean() - _t4_arr[_half:].mean())
_p5a_ok = (_drift_1 <= _P5A_DRIFT_RATIO * _t1_arr.mean()) and (_drift_4 <= _P5A_DRIFT_RATIO * _t4_arr.mean())
print(f"P5-a診断: 条件1のドリフト={_drift_1:.4f}s({_drift_1 / _t1_arr.mean():.1%}), "
      f"条件4のドリフト={_drift_4:.4f}s({_drift_4 / _t4_arr.mean():.1%}) -> "
      f"{'成立' if _p5a_ok else '不成立'}(閾値{_P5A_DRIFT_RATIO:.0%})")

_tau = float(_t4_arr.mean() / _t1_arr.mean())
_se_t1, _se_t4 = _t1_arr.std(ddof=1) / np.sqrt(len(_t1_arr)), _t4_arr.std(ddof=1) / np.sqrt(len(_t4_arr))
# 比の標準誤差の誤差伝播(近似): sigma_tau/tau ~ sqrt((se_t4/t4)^2 + (se_t1/t1)^2)
_sigma_tau = _tau * math.sqrt((_se_t4 / _t4_arr.mean()) ** 2 + (_se_t1 / _t1_arr.mean()) ** 2)

# 前提条件 P5-b: 平均の標準誤差が平均の10%以下であること。
_P5B_SE_RATIO = 0.10
_p5b_ok = (_se_t1 <= _P5B_SE_RATIO * _t1_arr.mean()) and (_se_t4 <= _P5B_SE_RATIO * _t4_arr.mean())
print(f"t1(平均)={_t1_arr.mean():.4f}s(se={_se_t1:.4f}, {_se_t1 / _t1_arr.mean():.1%}), "
      f"t4(平均)={_t4_arr.mean():.4f}s(se={_se_t4:.4f}, {_se_t4 / _t4_arr.mean():.1%})")
print(f"P5-b診断: {'成立' if _p5b_ok else '不成立'}(閾値{_P5B_SE_RATIO:.0%})")

precondition_status["P5-a"] = bool(_p5a_ok)
precondition_status["P5-b"] = bool(_p5b_ok)

verdict_H = judge_three_way(1 - _tau, 2 * _sigma_tau)
print(f"{_smoke_tag}tau={_tau:.4f}, sigma_tau={_sigma_tau:.4f}, 判定={verdict_H}")

```

    WARMUP_ITERATIONS_H=5, NUM_ITERATIONS_H=30
    次元が8の倍数か: D_MODEL(256)=True, D_FF(1024)=True, SWIGLU_D_FF(683)=False
    P5-a診断: 条件1のドリフト=0.0003s(0.3%), 条件4のドリフト=0.0017s(2.1%) -> 成立(閾値10%)
    t1(平均)=0.1113s(se=0.0001, 0.1%), t4(平均)=0.0793s(se=0.0003, 0.4%)
    P5-b診断: 成立(閾値10%)
    tau=0.7128, sigma_tau=0.0032, 判定=支持


### 6.15 判定結果一覧



```python
_experiment_preconditions = {
    "A": [], "B": [], "C": ["P0", "P1", "P2"], "D": ["P0", "P1", "P2", "P3"], "E": [],
    "F": ["P4"], "G": [], "H": ["P5-a", "P5-b"],
}


def _preconditions_summary(names: list[str]) -> str:
    if not names:
        return "-"
    return ", ".join(f"{n}={'成立' if precondition_status.get(n) else '不成立'}" for n in names)


def _final_verdict(name: str, computed_verdict: str) -> str:
    # 実験の前提条件が 1 つでも不成立なら、判定関数の計算結果に関わらず
    # 「前提不成立」とする(判定不能と区別する)。判定関数自体の計算結果は
    # 参考値として別欄(判定関数の結果(参考))に残す。
    _names = _experiment_preconditions[name]
    if _names and not all(precondition_status.get(n) for n in _names):
        return "前提不成立"
    return computed_verdict


if experiment_g_rho is not None:
    # 宣言した判定基準(rho < 0.75 で支持、それ以外は反証、6.4 節)を適用する。
    verdict_G = "支持" if experiment_g_rho < 0.75 else "反証"
    _g_stat = f"rho={experiment_g_rho:.4f}"
else:
    verdict_G = "-"
    _g_stat = "MPSのためコード経路のみ確認"
_g_stat += f" / パラメータ側メモリ許容誤差内={experiment_g_param_memory_ok}"

_verdict_table = [
    ("A", "定性的観察のため判定基準なし", "-"),
    ("B", f"fp16>=bf16 全体: {_fp16_ge_bf16_everywhere}, 合計fp16>bf16: {_fp16_sum_gt_bf16_sum}", "-"),
    ("C", f"D_C={D_C}", verdict_C),
    ("D", f"D_D={D_D}", verdict_D),
    ("E", f"全水準理論と一致: {_all_match_theory}", "-"),
    ("F", f"D_F={D_F:.4f}(新基準)/ 旧基準判定={verdict_F_old}", verdict_F),
    ("G", _g_stat, verdict_G),
    ("H", f"tau={_tau:.4f}", verdict_H),
]
print("実験 | 対比量など | 前提条件の成否 | 判定関数の結果(参考) | 最終判定")
for _name, _stat, _computed_verdict in _verdict_table:
    _precond_str = _preconditions_summary(_experiment_preconditions[_name])
    _final = _final_verdict(_name, _computed_verdict)
    print(f"  {_name}: {_stat} | 前提条件: {_precond_str} | {_computed_verdict} | {_final}")
print()
print(f"前提条件の成否一覧(P0: 5.8節, P1-P3: 6.8.1節, P4: 6.12節, P5: 6.14節): {precondition_status}")
if SMOKE_TEST:
    print("上記はスモークテスト(SMOKE_TEST=True)によるコードの動作確認結果であり、"
          "本番実行(SMOKE_TEST=False、Google Colab T4)後に数値が変わるため、"
          "支持・反証といった結論としては扱わない(第1段階では判定を確定させない)。")

```

    実験 | 対比量など | 前提条件の成否 | 判定関数の結果(参考) | 最終判定
      A: 定性的観察のため判定基準なし | 前提条件: - | - | -
      B: fp16>=bf16 全体: True, 合計fp16>bf16: True | 前提条件: - | - | -
      C: D_C=0.045814454298842566 | 前提条件: P0=成立, P1=成立, P2=成立 | 支持 | 支持
      D: D_D=0.04544379323877168 | 前提条件: P0=成立, P1=成立, P2=成立, P3=成立 | 支持 | 支持
      E: 全水準理論と一致: True | 前提条件: - | - | -
      F: D_F=0.6967(新基準)/ 旧基準判定=支持 | 前提条件: P4=成立 | 支持 | 支持
      G: rho=1.0413 / パラメータ側メモリ許容誤差内=True | 前提条件: - | 反証 | 反証
      H: tau=0.7128 | 前提条件: P5-a=成立, P5-b=成立 | 支持 | 支持
    
    前提条件の成否一覧(P0: 5.8節, P1-P3: 6.8.1節, P4: 6.12節, P5: 6.14節): {'P0': True, 'P1': True, 'P2': True, 'P3': True, 'P4': True, 'P5-a': True, 'P5-b': True}


## 7. 結果・考察 / Results and Discussion

本番実行(Google Colab T4、torch 2.13.0+cu130、`SMOKE_TEST=False`)の **最終実行**
(可視化セルを追加した後の実行)のセル出力に基づいて、結果・考察を記す。

- 7.0 節は第 1 回本番実行の記録であり、当時の判定表・前提条件の成否をそのまま残す。
- 7.1〜7.10 節には、事前宣言した判定基準による結論のみを書く。
- 結果を見た後に立てた解釈は、見出しで事後的であることを明示した 7.9.1 節と、
  7.12 節の該当段落に分けて書く。事後的な解釈は検証済みの結論として扱わない。

### 7.0 第1回本番実行の記録

**この節は、判定基準・前提条件の定義・閾値を一切変更せず、第1回本番実行
(Google Colab T4、`SMOKE_TEST=False`)の出力をそのまま転記した記録である。
結果・考察の本文(7.1 節以降)は、その後の再実行(最終実行)の出力と
突き合わせて執筆した。**

#### 7.0.1 判定表(実験 A〜H)

| 実験 | 対比量など | 前提条件の成否 | 判定関数の結果(参考) | 最終判定 |
|---|---|---|---|---|
| A | 定性的観察のため判定基準なし | - | - | - |
| B | fp16>=bf16 全体: True, 合計fp16>bf16: True | - | - | - |
| C | $D_C=0.045020759838926505$ | P0=成立, P1=成立, P2=成立 | 支持 | 支持 |
| D | $D_D=0.044621561904122586$ | P0=成立, P1=成立, P2=成立, P3=成立 | 支持 | 支持 |
| E | 全水準理論と一致: True | - | - | - |
| F | $D_F=0.6967$(新基準)/ 旧基準判定=支持 | P4=成立 | 支持 | 支持 |
| G | $\rho=1.0413$ / パラメータ側メモリ許容誤差内=False | - | -(注1) | - |
| H | $\tau=0.7024$, $\sigma_{\tau}=0.0034$ | P5-a=成立, P5-b=成立 | 支持 | 支持 |

(注1)第1回実行時点の 6.15 節のコードは、実験 G について判定関数の結果を
計算せず、常に`"-"`を出力していた(後述する 2 の不備)。宣言済みの判定基準
($\rho < 0.75$ なら支持、そうでなければ反証、6.4 節)をこの $\rho=1.0413$ に
そのまま適用すると **反証** となる。第1回の出力欄が`"-"`だったことは、
判定基準を満たさなかった結果ではなく、判定表の実装が判定関数を呼び出して
いなかったことによるものである。

前提条件 P0〜P5-b の成否は次のとおりで、すべて成立していた。

```
{'P0': True, 'P1': True, 'P2': True, 'P3': True, 'P4': True, 'P5-a': True, 'P5-b': True}
```

#### 7.0.2 実験 G の $\rho$ と系列長ごとのピークメモリ

| 条件 | 系列長 | ピークメモリ(bytes) |
|---|---|---|
| 条件1(FP32) | 256 | 2,222,404,096 |
| 条件1(FP32) | 512 | 4,271,092,224 |
| 条件1(FP32) | 1024 | 11,209,191,936 |
| 条件4(自動混合精度) | 256 | 2,069,788,160 |
| 条件4(自動混合精度) | 512 | 4,116,953,600 |
| 条件4(自動混合精度) | 1024 | 11,427,297,792 |

$\rho = \dfrac{11{,}427{,}297{,}792 - 2{,}069{,}788{,}160}{11{,}209{,}191{,}936 - 2{,}222{,}404{,}096} \approx 1.0413$

#### 7.0.3 実験 G のパラメータ側メモリの確認

理論値 = 48.3 MB、実測値 = 1079.3 MB、許容誤差(20%)以内 = False。

この実測値は、後述する 3 の実装上の誤り(`torch.cuda.memory_allocated()`の
絶対値を測定したため、6.7 節・6.12 節で構築済みの他モデルの解放されずに
残っていた確保量が混入していた)によるものであり、理論値との比較自体が
成立していなかった。詳細な原因と修正内容は 6.13 節に記録する。

#### 7.0.4 再実行に至った理由

以下の項目は、いずれも観測結果の方向とは無関係に特定できる、実装上の不備・
追加である(結果を見た後に判定基準を変更するものではない)。

- 6.15 節の判定表が、実験 G について判定関数の結果を計算せず`"-"`を出力
  していた不備、CUDA 以外の環境向けの文言が MPS 固有の表現になっていた
  不備、および本節末尾の「スモークテスト」の注記が`SMOKE_TEST`の値に
  関わらず出力されていた不備を修正した。この最後の不備により、
  第1回本番実行(`SMOKE_TEST=False`)の実際の出力にも
  「上記はスモークテスト(SMOKE_TEST=True)による…」という誤った注記が
  印字されていた。
- 実験Gのパラメータ側メモリ確認の測定方法(7.0.3 節)に実装上の誤りが
  あったため修正した(6.13 節)。
- 実験Gに、事後的な診断(保存されるテンソルの dtype 内訳)を追加した
  (6.13 節)。判定基準の変更ではなく、診断量の追加である。

**再実行の結果を正とする。判定が第1回と食い違う実験があれば両方を報告し、
その実験は実行間で判定が安定しなかったものとして扱う。**

### 7.1 較正結果

較正はすべて本番スケール(300 ステップ、バッチサイズ 32、系列長 256)・seed 0 で
行った(6.2 節)。

| 較正対象 | 実測値 | 決め方 |
|---|---|---|
| `GRADIENT_CLIP_THRESHOLD` | 1.1187 | 条件1(FP32)の較正実行の勾配ノルムの 0.85 分位点 |
| `STATIC_SCALE` | $2^{16} = 65536$ | 活性化勾配の高い分位点 $A_{\text{high}}$ と安全マージン $2BT$ から決める規則 |
| `DYNAMIC_SCALER_KWARGS` | `init_scale=65536`、`growth_factor=2.0`、`backoff_factor=0.5`、`growth_interval=40` | 初期値を`STATIC_SCALE`と同じにし、300 ステップの中で growth が複数回起こりうる間隔を選ぶ |
| `K_EXPERIMENT_F` | 60 | 5.3 節で宣言した値を、較正実行で前提条件 P4 が成立することにより確認 |

**gradient clipping の閾値**: 条件1の較正実行の勾配ノルムは平均 0.9299・標準偏差
0.5758・ピーク / 平均比 4.7289 であり、その 0.85 分位点として 1.1187 を得た。
この較正実行の最終訓練損失は 2.0224 で、前提条件 P1 の閾値
$\ln V \times 0.60 = 2.5046$ を下回った(参考値。本番の判定は 7.2 節)。

**静的損失スケールの探索的検証**: 初期化直後のモデル(seed 0)で、候補スケール
$S = 2^{14}$〜$2^{20}$ を 1 ステップずつ試した。$S = 2^{14}, 2^{15}, 2^{16}, 2^{17}$
ではパラメータの勾配はすべて有限であり、$S = 2^{18}$ 以上($2^{18}, 2^{19}, 2^{20}$)
で非有限な勾配が生じた。

**静的損失スケールの決定**: 条件1相当の学習(300 ステップ)で、自己注意機構・
順伝播ネットワークの出力に対する勾配の絶対値の 99.9% 分位点をステップごとに求め、
その最大値として $A_{\text{high}} = 4.104892 \times 10^{-5}$ を得た。安全マージン
$2BT = 2 \times 32 \times 256 = 16384$ を用いると、

$$
\frac{65504}{4.104892 \times 10^{-5} \times 16384} \approx 9.740 \times 10^{4}
$$

であり、これを超えない最大の 2 のべき $2^{16} = 65536$ を`STATIC_SCALE`とした。
この値は、探索的検証で非有限な勾配が生じ始めた $2^{18}$ より 2 段階小さく、
「探索的検証で非有限な勾配を生じさせないこと」のアサーションを通過した。

**前提条件の較正段階での成否**(いずれも参考値。本番の判定は 7.2 節で、判定に使う
実行そのものから評価する):

- P1: 較正実行の最終訓練損失 2.0224 ≤ 2.5046 で成立。
- P2: 較正実行(seed 0)の厳密な 0 の比率は条件1で 0.000000、条件2で 0.017184、
  マージン 0.017184 が最小マージン 0.005 以上で成立。
- P3: 6.3 節の時間見積もりのための本番スケール条件4(seed 0)で、backoff 2 回・
  growth 7 回が起き、成立する見込みを確認した。
- P4: $K = 60$ で条件5の発動率 $q_5 = 0.3167$(19/60)が範囲 $[0.02, 0.98]$ に
  入り成立(条件6は $q_6 = 1.0000$、60/60)。

較正段階で不成立となった前提条件はなく、較正値の見直しは行っていない。

### 7.2 前提条件 P0〜P5-b の成否

最終実行では、宣言したすべての前提条件が成立した。したがって、7.5〜7.10 節の
判定に「前提不成立」の実験はない。

| 前提条件 | 対象の実験 | 評価に使った量(最終実行) | 閾値 | 成否 |
|---|---|---|---|---|
| P0 | C・D | 5.8 節のモデル全体での確認で、全 4 層の自己注意機構・順伝播ネットワークの出力が FP16 | 2 項目がすべて成り立つ | 成立 |
| P1 | C・D | 条件1の全 5 シードの最終訓練損失 1.9452・1.9166・1.9116・1.9276・1.9037 | 2.5046 以下 | 成立 |
| P2 | C・D | 厳密な 0 の比率のシード平均: 条件1 0.000000、条件2 0.017641(マージン 0.017641) | マージン 0.005 以上 | 成立 |
| P3 | D | 条件4の全 5 シードで backoff 1〜2 回・growth 7 回 | それぞれ 1 回以上 | 成立 |
| P4 | F | 条件5の全 5 シードの $q_5$ = 0.3167・0.2833・0.3167・0.3000・0.3000 | $[0.02, 0.98]$ | 成立 |
| P5-a | H | 前半と後半の平均の差: 条件1 0.3%、条件4 2.1% | 全体平均の 10% 以内 | 成立 |
| P5-b | H | 平均の標準誤差: 条件1 0.1%、条件4 0.4% | 平均の 10% 以下 | 成立 |

- **P0**: T4(CUDA)上で、P0 の判定に使う 2 項目に加え、診断量として印字した残りの
  4 項目(RMSNorm の出力が FP32、Decoder Block の出力(残差接続後)が FP32、logits が
  FP16、loss が FP32)もすべて成り立った。単体のテンソル演算でも、autocast 下の
  `pow`が FP32 に昇格すること、FP32 と FP16 の加算が FP32 になることを確認した。
  3.6 節で MPS 上の実測として述べた 2 つの機構は、T4 上でも成り立った。さらに 6.7 節の
  アサーションにより、判定に使う学習そのものでも、サブレイヤー出力の dtype が
  条件1では全シードで FP32 のみ、条件2〜4では全シードで FP16 のみであった。
- **P2** は、較正実行ではなく、**判定に使う学習そのもの(6.7 節の条件1・条件2の
  全シード)から記録した診断量** で評価した。条件1で厳密な 0 になった要素は、
  各シード 5,033,164,800 要素のうち 9〜20 個であった。条件2では、同じ要素数のうち
  各シード 85,095,864〜91,768,856 個(1.69%〜1.82%)が厳密な 0 になった。

**実行間での判定の一致**: 本番実行は、次の 4 回を行った。

| 実行 | 内容 |
|---|---|
| 第 1 回 | 全セルを実行(7.0 節) |
| 第 2 回 | 6.13 節の事後的な診断の途中で GPU メモリ不足により中断(実験 G の $\rho$ とパラメータ側メモリの確認までは完了。実験 H と 6.15 節の判定表は未実行) |
| 第 3 回 | 全セルを実行 |
| 最終実行 | 可視化セルを追加して全セルを実行(本節以降の数値の出典) |

前提条件は、各回で実行された範囲ですべて成立した。判定も、実行された範囲で全回一致した。

- 実験 C・D・F: 全 4 回で支持。
- 実験 H: 実行された 3 回(第 1 回・第 3 回・最終実行)で支持($\tau$ = 0.7024・0.7082・0.7128)。
- 実験 G: $\rho = 1.0413$ が全 4 回で一致した。第 1 回の判定表は、7.0 節の注1の不備により
  「-」を出力していたが、宣言済みの判定基準を適用すると全回で反証となる。
- 実験 B・E: 判定の根拠となる真偽値が全 4 回で一致した。

### 7.3 実験 A(浮動小数点形式の表現特性)の結果

定性的観察のため判定基準は設けていない(6.4 節)。実測値(`torch.finfo`と
ビット操作、6.5 節)と、3.2 節の式から求めた理論値の対応を示す。理論値は、指数の
ビット数 $e$・仮数のビット数 $m$・$\mathrm{bias} = 2^{e-1} - 1$ から、最小正規数
$2^{1-\mathrm{bias}}$、最小非正規数 $2^{1-\mathrm{bias}-m}$、最大値
$(2 - 2^{-m}) \times 2^{\mathrm{bias}}$、unit roundoff $u_r = 2^{-m-1}$ として求めた。

| 形式 | 最小正規数 | 最小非正規数 | 最大値 | unit roundoff $u_r$ |
|---|---|---|---|---|
| FP32 | $1.175494 \times 10^{-38}$ ($2^{-126}$) | $1.401298 \times 10^{-45}$ ($2^{-149}$) | $3.402823 \times 10^{38}$ | $5.960464 \times 10^{-8}$ ($2^{-24}$) |
| FP16 | $6.103516 \times 10^{-5}$ ($2^{-14}$) | $5.960464 \times 10^{-8}$ ($2^{-24}$) | $65504$ | $4.882812 \times 10^{-4}$ ($2^{-11}$) |
| BF16 | $1.175494 \times 10^{-38}$ ($2^{-126}$) | $9.183550 \times 10^{-41}$ ($2^{-133}$) | $3.389531 \times 10^{38}$ | $3.906250 \times 10^{-3}$ ($2^{-8}$) |

実測値は、表のすべての欄で理論値と一致した。アサーションで自動確認したのは、FP16 の
最小正規数・最小非正規数・unit roundoff と、BF16 の最小正規数が FP32 と一致すること
である。残りの欄は、本節で理論式と照合した。

**表現範囲と相対精度のトレードオフ**: FP16 の正規化数の範囲は $2^{-14}$ から 65504
(約 $2^{16}$)までの約 $2^{30}$ 倍にとどまる。一方、BF16 の範囲は FP32 と同じく
$2^{-126}$ から約 $2^{128}$ まで広がる。その代わり、BF16 の unit roundoff
$2^{-8}$ は FP16 の $2^{-11}$ の 8 倍であり、相対精度は FP16 より低い。指数の
ビット数が表現範囲を、仮数のビット数が相対精度を決めるという 3.2 節の整理どおりの
結果である。

**相対丸め誤差の曲線**(6.5 節の図、$10^{-8}$〜$10^{4}$ の値を往復キャスト):

- FP16 の相対誤差は、入力の絶対値が約 $3 \times 10^{-8}$ を下回る範囲で 1 に張り付く。
  これは、最小非正規数 $2^{-24} \approx 5.96 \times 10^{-8}$ の半分未満の値が 0 に
  丸められる(アンダーフロー)ためである。
- 約 $6 \times 10^{-8}$ から最小正規数 $6.1 \times 10^{-5}$ までの非正規数の範囲では、
  相対誤差が大きく不規則に変動しながら減少する。値が小さいほど仮数の有効桁数が
  減る段階的アンダーフローの様子である。
- 最小正規数より大きい範囲では、FP16 の相対誤差は図の縦軸の尺度ではほぼ 0 に見える。
- BF16 の相対誤差は、描画した全範囲でほぼ 0 に見える。この範囲の値はすべて BF16 の
  正規化数に収まるためである。

縦軸が線形であるため、正規化数の範囲での相対精度の差(FP16 の上界 $2^{-11}$ と
BF16 の上界 $2^{-8}$)は、この図からは読み取れない。トレードオフの相対精度の側は、
上の表の unit roundoff で確認した。また、描画範囲の上限 $10^{4}$ は FP16 の最大値
65504 に届かないため、オーバーフローの側はこの図に現れていない。

### 7.4 実験 B(活性化の勾配のアンダーフロー率)の判定結果

**判定: 支持。**

条件1(FP32)の学習(seed 0、300 ステップ)で、60 ステップごとの 5 つの記録ステップ
について、8 個のサブレイヤー(4 層 × 自己注意機構・順伝播ネットワーク)の出力に
対する勾配のアンダーフロー率を測定した。全層で平均した値は次のとおりである。

| 記録ステップ | 60 | 120 | 180 | 240 | 300 |
|---|---|---|---|---|---|
| $r_{\mathrm{FP16}}$(全層平均) | 0.02025 | 0.01698 | 0.01433 | 0.01418 | 0.01351 |
| $r_{\mathrm{BF16}}$(全層平均) | 0 | 0 | 0 | 0 | 0 |

$r_{\mathrm{BF16}}$ は、全層・全記録ステップ(8 × 5 = 40 点)のすべてで 0 であった。
このため、「全層・全記録ステップで $r_{\mathrm{FP16}} \ge r_{\mathrm{BF16}}$」と
「合計で $r_{\mathrm{FP16}} > r_{\mathrm{BF16}}$」がいずれも成り立ち、事前宣言した
判定基準により **支持** となる。

**BF16 のアンダーフロー率が 0 である理由(3.2 節・3.9 節との対応)**: 勾配は FP32 で
計算され、そこから各形式へキャストされる。BF16 の指数部は FP32 と同じ 8 ビットであり、
最小正規数も FP32 と同じ $2^{-126}$ である(実験 A)。したがって、FP32 の正規化数は
すべて BF16 の正規化数の範囲に収まる。BF16 へのキャストで 0 になりうるのは、元々
FP32 の非正規数(絶対値が $2^{-126}$ 未満)である値のうち、BF16 の最小非正規数
$2^{-133}$ の半分を下回るものに限られる。一方、FP16 では最小非正規数 $2^{-24}$ の
半分(約 $3 \times 10^{-8}$)を下回る値が 0 に丸められる。7.1 節で求めた活性化勾配の
99.9% 分位点の最大値 $A_{\text{high}} = 4.1 \times 10^{-5}$ は、FP16 の最小正規数
$6.1 \times 10^{-5}$ さえ下回る。つまり、活性化勾配の大部分は FP16 では非正規数の
範囲かそれ以下にあり、その裾が FP16 のアンダーフローの境界にかかる。これに対し、
BF16 の境界はそれより 30 桁以上小さい。3.9 節で述べた「BF16 は表現範囲が FP32 と
同等であるため、損失スケーリングが原理的に不要になる」という理論と対応する結果である。

**診断量について**: 6.4 節で診断量として宣言した「非正規数に落ちる要素の比率」
「パラメータの勾配についての同じ量」は、6.6 節のコードでは測定していない。このため、
本節では報告できない(判定基準の対比量には含まれないため、判定には影響しない)。
なお、この実験は seed 0 の 1 シードのみで行った。

### 7.5 実験 C(静的損失スケーリング)の判定結果

**判定: 支持。**

| 条件 | 最終 bits-per-byte(シードごと) | シード平均 $b_i$ | 標準誤差 $\sigma_i$ |
|---|---|---|---|
| 1(FP32) | 2.8529・2.8247・2.8687・2.8427・2.8377 | 2.8454 | 0.0074 |
| 2(FP16、損失スケーリングなし) | 2.8811・2.8702・2.9206・2.8856・2.8988 | 2.8912 | 0.0086 |
| 3(FP16、静的損失スケーリング) | 2.8531・2.8247・2.8687・2.8427・2.8380 | 2.8454 | 0.0074 |

対比量は $D_C = b_2 - b_3 = 0.0458$ である。閾値は
$2\sigma_{D_C} = 2\sqrt{\sigma_2^2 + \sigma_3^2} = 0.0227$ であり、
$D_C > 2\sigma_{D_C}$ を満たすため、事前宣言した判定基準により **支持** となる。
条件2・条件3のいずれにも、損失または評価値が非有限値になったシードはなかった。

条件3の最終 bits-per-byte は、シードごとに見ても条件1とほぼ一致した。seed 1・2・3 は
小数点以下 4 桁まで同じ値であり、seed 0・4 でも差は 0.0003 以下であった。シード平均も
4 桁で同じ 2.8454 である。一方、条件2は条件1より平均で 0.0458 高い。条件2の最小値
2.8702 は、条件1の最大値 2.8687 を上回った。

ただし、「条件3が条件1と一致する」ことは事前宣言した対比量ではない
(判定の対比量は $b_2 - b_3$)。この一致は観測として記すにとどめ、同等性を
検定した結論としては扱わない。

**直接の作用点(診断量)**: 損失スケーリングが直接作用する、サブレイヤー出力に対する
勾配の厳密な 0 の比率(6.7 節)は次のとおりであった。

- 条件2: 各シード 1.69%〜1.82%。
- 条件3: 各シード 1,268〜1,341 個(5,033,164,800 要素中、約 $2.6 \times 10^{-7}$)。
- 条件1: 各シード 9〜20 個。

静的損失スケーリングにより、アンダーフローによる厳密な 0 は条件2の約 5 桁下まで
減っている。対比量(最終 bits-per-byte)に現れた差は、この上流の量の差と方向が一致する。

### 7.6 実験 D(動的損失スケーリング)の判定結果

**判定: 支持。**

条件4(FP16、動的損失スケーリング)の最終 bits-per-byte は、シードごとに
2.8517・2.8258・2.8695・2.8433・2.8386 であった(シード平均 $b_4 = 2.8458$、
標準誤差 $\sigma_4 = 0.0073$)。条件2(7.5 節、$b_2 = 2.8912$、$\sigma_2 = 0.0086$)
との対比量は $D_D = b_2 - b_4 = 0.0454$ である。閾値は
$2\sigma_{D_D} = 2\sqrt{\sigma_2^2 + \sigma_4^2} = 0.0226$ であり、
$D_D > 2\sigma_{D_D}$ を満たすため、事前宣言した判定基準により **支持** となる。
条件4で損失または評価値が非有限値になったシードはなかった。

**スケール値の時系列(診断量)**:

| seed | backoff 回数 | growth 回数 | 更新をスキップしたステップの比率 | スケール値の範囲 |
|---|---|---|---|---|
| 0 | 2 | 7 | 0.0067(2/300) | $[2^{16}, 2^{22}] = [65536, 4194304]$ |
| 1 | 1 | 7 | 0.0033(1/300) | $[2^{16}, 2^{22}]$ |
| 2 | 2 | 7 | 0.0067(2/300) | $[2^{16}, 2^{22}]$ |
| 3 | 2 | 7 | 0.0067(2/300) | $[2^{16}, 2^{22}]$ |
| 4 | 2 | 7 | 0.0067(2/300) | $[2^{16}, 2^{22}]$ |

全シードで、スケール値は初期値 $2^{16}$ から始まった。学習の前半は、非有限値が
検出されないまま`growth_interval`(40 ステップ)ごとに 2 倍になった。後半は、
backoff と growth を繰り返しながら最大 $2^{22}$ に達した(時系列の形は 7.11 節の
図 5 で述べる)。更新を
スキップしたステップの数は、各シードの backoff 回数と一致した(非有限値を検出した
ステップで更新をスキップし、同時に backoff する手続き(3.4 節)どおり)。スキップの
比率は最大でも 0.67% であった。

サブレイヤー出力に対する勾配の厳密な 0 は、各シード 385〜466 個であった
(5,033,164,800 要素中)。条件2の 1.69%〜1.82% から大きく減っており、条件3
(1,268〜1,341 個)よりもさらに少ない。

### 7.7 実験 E(FP32 マスター重み)の判定結果

**判定: 支持。**

$w_0 = 1$ に一定の更新量 $u$ を $N = 100$ 回加算した(6.11 節)。3.5 節の加算方向の
境界により、$u / w_0 \le 2^{-11}$ で条件7(FP16 で直接更新)の更新が失われると
予測される。

| $u/w_0$ | 条件7 の最終値 | 更新消失の予測 / 観測 | 条件8(FP32 マスター重み)の最終値 | 理論値 $w_0 + Nu$ | 条件8 の FP16 の写し(診断量) |
|---|---|---|---|---|---|
| $2^{-8}$ | 1.3906250000 | なし / なし | 1.3906250000 | 1.3906250000 | 1.3906250000 |
| $2^{-9}$ | 1.1953125000 | なし / なし | 1.1953125000 | 1.1953125000 | 1.1953125000 |
| $2^{-10}$ | 1.0976562500 | なし / なし | 1.0976562500 | 1.0976562500 | 1.0976562500 |
| $2^{-11}$ | 1.0000000000 | あり / あり | 1.0488281250 | 1.0488281250 | 1.0488281250 |
| $2^{-12}$ | 1.0000000000 | あり / あり | 1.0244140625 | 1.0244140625 | 1.0244140625 |
| $2^{-13}$ | 1.0000000000 | あり / あり | 1.0122070312 | 1.0122070312 | 1.0117187500 |
| $2^{-14}$ | 1.0000000000 | あり / あり | 1.0061035156 | 1.0061035156 | 1.0058593750 |

- **条件7**: 全 7 水準で、更新の消失の有無が理論の予測と一致した。
  $u / w_0 = 2^{-11}$(ちょうど ulp の半分)で更新が失われたことは、round-to-nearest-even
  により $w_0$ 側に丸められるという 3.5 節の導出どおりである。
- **条件8**: 全水準で、FP32 マスター重み自身は理論値と誤差 0 で一致した
  (上界 $1.19 \times 10^{-5}$ 以内)。

以上から、事前宣言した判定基準により **支持** となる。

**FP16 の写しに乗る量子化誤差(診断量)**: 条件8の FP16 の写しは、$u/w_0 \ge 2^{-12}$
では FP32 マスター重みと一致した。$w_0 + Nu$ が $[1, 2)$ の区間の FP16 の格子
(間隔 $2^{-10}$)の上にちょうど乗るためである(例: $100 \times 2^{-12} = 25 \times
2^{-10}$)。

$u/w_0 = 2^{-13}$・$2^{-14}$ では、$w_0 + Nu$ が格子点の間に来る。このため、写しは
マスター重みより小さい最も近い格子点に丸められた。

- $2^{-13}$: $100 \times 2^{-13} = 12.5 \times 2^{-10}$ が、ちょうど中間から偶数側の
  $12 \times 2^{-10}$ に丸められ、誤差は $2^{-11} \approx 4.9 \times 10^{-4}$。
- $2^{-14}$: $6.25 \times 2^{-10}$ が $6 \times 2^{-10}$ に丸められ、誤差は
  $2^{-12} \approx 2.4 \times 10^{-4}$。

いずれも ulp の半分($2^{-11}$)以下であり、マスター重み側で更新が正しく蓄積されて
いても、読み出した写しには FP16 粒度の誤差が乗るという 6.4 節の注記どおりである。

なお、本実験で扱ったのは加算方向のみである。3.5 節で導出した減算方向の境界
($u/w_0 = 2^{-12}$)は、本実験では数値的に検証していない。

### 7.8 実験 F(gradient clipping の順序)の判定結果

**判定: 支持。**

先頭 $K = 60$ ステップでの clipping の発動率は次のとおりであった。

| seed | 0 | 1 | 2 | 3 | 4 | 平均 |
|---|---|---|---|---|---|---|
| $q_5$(正しい順序: unscale → clipping) | 0.3167 | 0.2833 | 0.3167 | 0.3000 | 0.3000 | 0.3033 |
| $q_6$(誤った順序: clipping → unscale) | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 |

対比量は $D_F = 0.6967$ である。$\sigma_{D_F} = 0.0062$(各条件のシード平均の
標準誤差から合成、新基準)であり、$D_F > 2\sigma_{D_F} = 0.0124$ を満たすため、
事前宣言した判定基準により **支持** となる。旧基準による参考値
($\sigma_{D_F}^{\text{old}} = 0.0139$)でも判定は支持で、変わらない。前提条件 P4
(全シードの $q_5$ が $[0.02, 0.98]$ の範囲内)は成立した。

条件6では、全シード・全 60 ステップで clipping が発動した。これは、3.7 節で述べた
「unscale 前のノルム $S\lVert g \rVert$ はほぼ常に $c$ を超える」という予測どおりで
ある($S = 65536$、$c = 1.1187$)。

**診断量(unscale 後の勾配ノルムの条件6 / 条件5 の比)**: シードごとに
$1.2714 \times 10^{-5}$・$1.3778 \times 10^{-5}$・$1.2917 \times 10^{-5}$・
$1.3988 \times 10^{-5}$・$1.3672 \times 10^{-5}$ であった。3.7 節の導出では、
誤った順序で clipping が発動したステップの最終的な勾配は
$g_{\text{final}} = \dfrac{c}{S\lVert g \rVert} g$ である。そのノルムは
$\lVert g \rVert$ によらず

$$
\lVert g_{\text{final}} \rVert = \frac{c}{S} = \frac{1.1187}{65536} \approx 1.707 \times 10^{-5}
$$

に固定される。条件6では全ステップで clipping が発動したため、比の分子(条件6の
unscale 後のノルムの平均)は $c/S$ に等しい。したがって、この比は
$(c/S) / \overline{\lVert g_5 \rVert}$ となる($\overline{\lVert g_5 \rVert}$ は、
条件5の先頭 60 ステップの unscale 後・clipping 前の勾配ノルムの平均)。

観測された比から逆算すると $\overline{\lVert g_5 \rVert} \approx 1.22$〜$1.34$ となり、
次の 2 点と矛盾しない。

- 条件5で約 30% のステップが閾値 $c = 1.1187$ を超えていること。
- 較正実行(300 ステップ)の勾配ノルムが平均 0.93・ピーク / 平均比 4.7 と、学習の
  序盤ほど大きいこと。

比が $1/S \approx 1.5 \times 10^{-5}$ の桁にあることは、3.7 節で導出した
$c/(S\lVert g \rVert)$ の水準と整合する。ただし、$\overline{\lVert g_5 \rVert}$
そのものはセル出力に印字していない。このため、これは逆算による整合性の確認であり、
独立な数値照合ではない。

### 7.9 実験 G(活性化メモリ)の判定結果

**判定: 反証。**

T4 上で、1 ステップ(順伝播 + 逆伝播)のピークメモリ(`torch.cuda.max_memory_allocated()`)
を測定した。条件はバッチサイズ 32、系列長 $L$ = 256・512・1024 である。

| 系列長 $L$ | 条件1(FP32)$M_1(L)$ | 条件4(混合精度)$M_4(L)$ | $M_4(L) - M_1(L)$ |
|---|---|---|---|
| 256 | 2,222,404,096 bytes | 2,069,788,160 bytes | −145.5 MB |
| 512 | 4,271,092,224 bytes | 4,116,953,600 bytes | −147.0 MB |
| 1024 | 11,209,191,936 bytes | 11,427,297,792 bytes | +208.0 MB |

(MB は $2^{20}$ バイト)

$$
\rho = \frac{M_4(1024) - M_4(256)}{M_1(1024) - M_1(256)}
= \frac{11{,}427{,}297{,}792 - 2{,}069{,}788{,}160}{11{,}209{,}191{,}936 - 2{,}222{,}404{,}096}
\approx 1.0413
$$

事前宣言した判定基準は「$\rho < 0.75$ で支持、それ以外は反証」である。
$\rho = 1.0413$ はこれを満たさないため、**反証** となる。この実装・この規模では、
`torch.autocast`による混合精度は、系列長の増加に伴うピークメモリの増分を削減
しなかった。系列長 256 から 1024 への増分は、条件4のほうが条件1より約 4.1% 大きかった。
3.8 節が予測した「活性化のメモリが FP16 化によりおよそ半分になる」ことは、
ピークメモリの増分としては観測されなかった。

**パラメータ側のメモリ**: 構築直前と、構築・順伝播・逆伝播・`AdamW.step()`の直後の
差分で測った(6.13 節)。3.8 節の内訳(重み 4 + 勾配 4 + AdamW の 1 次・2 次モーメント
推定 8 = 16 バイト / パラメータ)による理論値 48.3 MB に対し、実測値は 48.4 MB で
あった。許容誤差 20% 以内に収まり、`torch.autocast`の経路ではパラメータ側のメモリが
FP32 学習と変わらないという 3.8 節の導出と一致した。

#### 7.9.1 事後的な解釈: $\rho > 1$ となった経緯(事前宣言した判定とは独立)

**この小節は、実験 G の結果を見た後に立てた事後的な解釈である。** 7.9 節の判定
(反証)は、事前宣言した判定基準のみから導いたものであり、本小節の内容によって
変わらない。本小節の解釈は検証済みの結論ではない。

6.13 節の事後的な診断で、順伝播 1 回で`torch.autograd`が逆伝播用に保存したテンソルの
バイト数を集計した。分類は、dtype 別、および最終 2 次元が系列長 × 系列長のもの
(以下「Attention 形状」)とそれ以外(以下「その他」)である。ストレージを共有する
テンソルは 1 回だけ数えた。

系列長 256 の水準は解釈に使わない。$d_{\mathrm{model}} = 256$ が系列長と一致し、
形状 $(B, L, d_{\mathrm{model}})$ の通常の活性化も Attention 形状に分類されてしまう
ためである(6.13 節の注記)。

| 系列長 | 条件 | Attention 形状 FP32 | Attention 形状 FP16 | その他 FP32 | その他 FP16 | 合計(int64・bool を含む) |
|---|---|---|---|---|---|---|
| 512 | 1 | 1,073,741,824 | 0 | 1,630,418,944 | 0 | 2,705,340,416 |
| 512 | 4 | 1,073,741,824 | 536,870,912 | 302,588,928 | 764,578,304 | 2,678,959,616 |
| 1024 | 1 | 4,294,967,296 | 0 | 3,248,175,104 | 0 | 7,547,598,848 |
| 1024 | 4 | 4,294,967,296 | 2,147,483,648 | 605,168,640 | 1,522,829,824 | 8,574,905,856 |

(単位はバイト)

**Attention 形状の保存テンソル**: FP32 のバイト数は、系列長 512・1024 のいずれでも
条件1と条件4で完全に一致した。条件4では、そこに FP16 のバイト数が上乗せされている。
これらを、層数 4 × バッチサイズ 32 × ヘッド数 8 × 系列長の 2 乗 × 要素あたりの
バイト数と比べると、次のようにすべて一致する。

$$
4 \times 32 \times 8 \times 512^2 \times 4 = 1{,}073{,}741{,}824, \qquad
4 \times 32 \times 8 \times 1024^2 \times 4 = 4{,}294{,}967{,}296
$$

$$
4 \times 32 \times 8 \times 512^2 \times 2 = 536{,}870{,}912, \qquad
4 \times 32 \times 8 \times 1024^2 \times 2 = 2{,}147{,}483{,}648
$$

つまり、どちらの条件でも、層ごとに (バッチ, ヘッド, 系列長, 系列長) の FP32 テンソルが
ちょうど 1 個保存されている。条件4では、それと同じ要素数の FP16 テンソルがもう 1 個
保存されている。**系列長の 2 乗で効く支配項は、要素あたり 4 バイト(条件1)から
6 バイト(条件4)に、1.5 倍に増えた。** 3.8 節が暗に想定した「4 バイトから 2 バイトへの
半減」とは逆向きである。

この数値は、次の解釈と整合する。

1. 6.13 節の単体呼び出しで、autocast 下の`scaled_dot_product_attention`では、softmax の
   出力(確率行列)自体が FP32、それを入力とする Value との行列積の出力が FP16 で
   あることを確認した。
2. FP32 の softmax 出力は softmax の逆伝播のために保存される。
3. 行列積が内部で FP16 にキャストした写しは、行列積の逆伝播(Value に対する勾配)の
   ために別に保存される。
4. 条件1では、softmax の逆伝播と行列積の逆伝播が同じ FP32 テンソルを参照するため、
   1 個として数えられる。

ただし、診断では保存テンソルの dtype・形状・バイト数のみを記録しており、どの演算が
どのテンソルを保存したかは直接記録していない。2〜4 は数値と矛盾しない解釈であって、
確認された事実ではない。

**その他の保存テンソル**: FP16 化による削減が起きていた。条件4 / 条件1 のバイト数の比
(条件4は FP32 と FP16 の合計)は、系列長 512 で
$1{,}067{,}167{,}232 / 1{,}630{,}418{,}944 \approx 0.655$、系列長 1024 で
$2{,}127{,}998{,}464 / 3{,}248{,}175{,}104 \approx 0.655$ であった。半分(0.5)まで
下がらないのは、条件4でもその他の一部(系列長 512 で 302,588,928 バイト、1024 で
605,168,640 バイト)が FP32 のまま保存されているためである。どのテンソルが FP32 の
まま残ったかは、この診断では特定していない。

**両者の合計**: 条件1の保存テンソルに占める Attention 形状の割合は、系列長 512 で
約 40%、1024 で約 57% であり、系列長とともに大きくなる。

| 系列長 | 合計の比(条件4 / 条件1) |
|---|---|
| 512 | 0.990(Attention 形状の +50% とその他の約 −35% がほぼ相殺) |
| 1024 | 1.136(Attention 形状の +50% が上回る) |

この傾向は、ピークメモリの差 $M_4(L) - M_1(L)$ の符号が系列長 512 までは負、1024 で
正に転じたこと(7.9 節の表)と **向きは一致する**。しかし、大きさは一致しない。

- 系列長 1024 の保存テンソルの差: +1,027,307,008 バイト(約 980 MB)
- 同じ系列長のピークメモリの差: +218,105,856 バイト(208.0 MB)

ピークメモリには、逆伝播中に一時的に確保されるテンソルなど、順伝播の保存テンソル
以外の要因も含まれる。このため、本診断は $\rho$ の値そのものを定量的に説明するもの
ではない。

**この解釈の適用範囲**: 以上は、本リポジトリの`scaled_dot_product_attention`
(確率行列を明示的に作り、逆伝播用に保存する実装)と、この PyTorch のバージョン
(2.13.0)・T4 上の`torch.autocast`の softmax の精度の扱いのもとでの事後的な解釈で
ある。確率行列を保存せず逆伝播時に再計算する Attention の実装や、softmax を FP16 の
まま計算する実装では、結果が変わりうる。「混合精度は活性化メモリを削減しない」という
一般化した主張はしない。

### 7.10 実験 H(学習スループット)の判定結果

**判定: 支持。**

ウォームアップ 5 反復の後、条件1・条件4を 1 反復ずつ交互に 30 反復計測した
(バッチサイズ 32、系列長 256)。

| 条件 | 1 ステップあたりの時間(平均) | 平均の標準誤差 |
|---|---|---|
| 1(FP32) | $t_1$ = 0.1113 s | 0.0001 s(0.1%) |
| 4(混合精度) | $t_4$ = 0.0793 s | 0.0003 s(0.4%) |

$\tau = t_4 / t_1 = 0.7128$、$\sigma_\tau = 0.0032$ であった。
$\tau < 1 - 2\sigma_\tau = 0.9936$ を満たすため、事前宣言した判定基準により **支持**
となる。混合精度により、1 ステップあたりの時間は約 29% 短縮した。

**前提条件**:

- P5-a: 前半と後半の平均の差が、条件1で 0.3%、条件4で 2.1% であり、閾値 10% 以内で
  成立した。
- P5-b: 平均の標準誤差が、条件1で平均の 0.1%、条件4で 0.4% であり、閾値 10% 以下で
  成立した。

**Tensor Core の利用条件(診断量)**:

- $d_{\mathrm{model}} = 256$・$d_{\mathrm{ff}} = 1024$ は 8 の倍数である。
- SwiGLU の中間次元 $\mathrm{round}(\tfrac{2}{3} d_{\mathrm{ff}}) = 683$(`SWIGLU_D_FF`)
  は 8 の倍数ではない。

したがって、順伝播ネットワーク(SwiGLU)の行列積は、T4 の Tensor Core を利用する
ための次元の条件を満たしていない可能性がある。ただし、この次元を 8 の倍数に揃えた
条件は計測していないため、$\tau$ にどの程度影響したかはこの実験からは分からない。

### 7.11 可視化

本番実行で描画した 6 枚の図(3 節の図 1 に続けて図 2〜図 7 と番号を付ける)について、何を描いた図か、そこから何が読み取れるかを
記す。

**図 2: 相対丸め誤差と値の大きさ(6.5 節、実験 A)**

- 何を描いた図か: $10^{-8}$〜$10^{4}$ の値を FP16・BF16 へ往復キャストしたときの
  相対丸め誤差を、入力の絶対値(対数軸)に対して描いた。
- 読み取れること: FP16 の曲線は、約 $3 \times 10^{-8}$ 以下で 1(完全なアンダーフロー)
  になる。そこから最小正規数 $6.1 \times 10^{-5}$ にかけて、不規則に大きな誤差を示しつつ
  減少する(非正規数の範囲での段階的アンダーフロー)。BF16 の曲線は、全範囲でほぼ 0 に
  張り付く。FP16 の表現範囲の狭さ(下側)を示す図である。
- この図からは読み取れないこと: 縦軸が線形のため、正規化数の範囲での相対精度の差
  (BF16 の $u_r$ が FP16 の 8 倍)は見えない(7.3 節)。

**図 3: 学習中のアンダーフロー率(6.6 節、実験 B)**

- 何を描いた図か: 条件1の学習中、各記録ステップでサブレイヤー出力の勾配を FP16・BF16
  にキャストしたときのアンダーフロー率を、8 個のサブレイヤーごとに 1 本の線で描いた。
- 読み取れること: BF16 の線は、すべてのサブレイヤー・記録ステップで 0 に重なる。
  FP16 の線はすべて 0 より上にあり、サブレイヤー間で値に幅がある。
  - 8 本中 6 本は、ステップ 60 の約 0.012〜0.023 から、ステップ 300 の約 0.004〜0.013
    へと学習の進行とともに減少する。
  - 1 本はほぼ横ばい(約 0.024)。
  - 1 本は約 0.026 から約 0.041 へ増加する。
- 注記: 凡例はサブレイヤーを区別していないため、どの層のどのサブレイヤーが
  増加・横ばいの線に当たるかは、この図からは特定できない。

**図 4: 条件ごとの最終 bits-per-byte(6.9 節、実験 C・D)**

- 何を描いた図か: 条件1〜4の最終 bits-per-byte をシードごとの点で描き、シード平均と
  最小値〜最大値の範囲を重ねた。破線は条件1の平均である。
- 読み取れること:
  - 条件3・条件4の点の分布は条件1とほぼ重なり、平均も破線上にある。
  - 条件2だけが全シードで上にずれ、最小〜最大の範囲が条件1の範囲と重ならない。
  - 損失スケーリングなしの FP16 だけが学習の質を落とし、静的・動的いずれの
    損失スケーリングでも FP32 と見分けのつかない水準に戻っている。

**図 5: 条件4の損失スケール値の時系列(6.10 節、実験 D)**

- 何を描いた図か: 5 シードの損失スケール値を対数軸で重ね描きし、更新をスキップした
  ステップを × 印で示した。
- 読み取れること:
  - ステップ 200 付近までは全シードの線が重なる。40 ステップごとに 2 倍になる階段状の
    growth が、非有限値の検出なしに続く($2^{16}$ から約 $2 \times 10^{6}$ すなわち
    $2^{21}$ まで)。
  - それ以降はシード間で線が分かれる。× 印(非有限値の検出と更新のスキップ)の
    ステップで $2^{20}$ への backoff が起き、別のステップでは $2^{22}$ への growth と、
    その直後の backoff が起きる。
  - スキップは学習の後半(ステップ 200 以降)にのみ現れた。スケール値が
    オーバーフローの境界付近に到達した後は、その近傍を行き来しながら追跡している。
    3.4 節で述べた「オーバーフローしない最大の値の近傍を自動的に追跡する」動作に
    当たる。

**図 6: 逆伝播のために保存されたテンソルのバイト数(6.13 節、実験 G の事後的な診断)**

- 何を描いた図か: 条件1・4 × 系列長 3 水準の各組み合わせについて、保存テンソルの
  バイト数を「Attention 形状 / その他」× dtype で積み上げた。
- 読み取れること:
  - 条件1では、系列長とともに Attention 形状(FP32)の部分が急速に大きくなる。
  - 条件4では、Attention 形状の FP32 の部分が条件1と同じ高さのまま残り、そこに
    Attention 形状の FP16 の部分が積み上がる。
  - 一方、その他の部分は、条件4では大半が FP16 に置き換わり、条件1より低い。
  - 系列長 1024 では、条件4の棒全体が条件1より高い(7.9.1 節)。
- 注記: 系列長 256 の棒の Attention 形状の分類には、6.13 節で述べた混入がある。

**図 7: 1 ステップあたりのピークメモリ(6.13 節、実験 G)**

- 何を描いた図か: 条件1・条件4のピークメモリを、系列長 3 水準についてグループ棒
  グラフで描いた。
- 読み取れること:
  - 系列長 256・512 では条件4がわずかに低いが、1024 では逆に条件4がわずかに高い。
  - どの系列長でも両条件の差はピークメモリ全体に比べて小さく、系列長に対する
    増え方はほぼ同じである。
  - 3.8 節が予測した「条件4の増え方が条件1のおよそ半分になる」様子は見られない
    ($\rho = 1.0413$、7.9 節)。

### 7.12 全体としての考察

**判定の一覧**(前提条件はすべて成立、7.2 節):

| 実験 | 検証したこと | 判定 |
|---|---|---|
| A | FP32・FP16・BF16 の表現特性 | 判定基準なし(実測値は理論値と一致) |
| B | FP16 は BF16 よりアンダーフローしやすい | 支持 |
| C | 静的損失スケーリングは最終 bits-per-byte を改善する | 支持 |
| D | 動的損失スケーリングは最終 bits-per-byte を改善する | 支持 |
| E | FP32 マスター重みは、FP16 では丸め落ちる更新を保持する | 支持 |
| F | unscale 前に clipping すると、clipping がほぼ毎ステップ発動する | 支持 |
| G | 混合精度は系列長に比例する活性化メモリの増分を削減する | **反証** |
| H | 混合精度は 1 ステップあたりの時間を短縮する | 支持 |

**損失スケーリング(静的・動的)**: 損失スケーリングなしの FP16(条件2)では、
サブレイヤー出力に対する勾配の 1.69%〜1.82% が厳密な 0 になった。最終 bits-per-byte
は FP32 より平均 0.0458 悪化した。静的($S = 2^{16}$)・動的のいずれの損失スケーリング
でも、厳密な 0 の比率は $10^{-7}$ 以下まで下がった。最終 bits-per-byte も、条件2との
差が閾値の約 2 倍($D_C = 0.0458$・$D_D = 0.0454$ に対し閾値 0.0227・0.0226)という
形で改善した(実験 C・D)。上流の量(厳密な 0 の比率)と下流の量(bits-per-byte)の
両方で、3.4 節の「逆伝播の間だけ勾配を $S$ 倍にずらしてアンダーフローを避ける」
仕組みが働いたことが確認できた。BF16 ではアンダーフロー自体が起きないこと
(実験 B)は、3.9 節の「BF16 では損失スケーリングが原理的に不要になる」ことと対応する。

**FP32 マスター重み**: 合成タスク(実験 E)で、FP16 の直接更新は $u/w_0 \le 2^{-11}$
で完全に失われ、FP32 マスター重みでは全水準で理論値どおりに蓄積された。3.5 節の
加算方向の境界がそのまま再現された。なお、`torch.autocast`を用いる本ノートブックの
学習経路(条件2〜4)では、パラメータが常に FP32 で保持される(3.8 節)。このため、
更新の丸め落ちはそもそも生じない。FP32 マスター重みの必要性は、FP16 の重みを実際に
保持する設計(Micikevicius et al. [1] の元の設計)に対する検証として、合成タスクで
のみ確認した。

**gradient clipping との順序**: unscale 前に clipping すると、全シード・全ステップで
clipping が発動した($q_6 = 1$、正しい順序では $q_5 \approx 0.30$)。unscale 後の
勾配ノルムは $c/S$ の水準に押し潰されていた(実験 F)。3.7 節で導出した「閾値が実質
$c/S$ に縮小する」誤りを再現し、処理順序「unscale → 非有限値の検出 → clipping」が
必要であることを支持した。

**メモリと学習時間**: `torch.autocast`による混合精度は、この実装・この規模では、
ピークメモリの系列長に対する増分を削減しなかった($\rho = 1.0413$、実験 G は反証)。
系列長 1024 では、ピークメモリが FP32 より 208.0 MB 大きかった。パラメータ側の
メモリは理論値 48.3 MB に対し実測 48.4 MB で、3.8 節の「autocast の経路では
パラメータ側は FP32 学習と変わらない」ことと一致した。一方、1 ステップあたりの時間は
FP32 の 0.1113 s から 0.0793 s へ短縮し、$\tau = 0.7128$(約 29% の短縮)であった
(実験 H)。この実装・この規模の混合精度は、メモリではなく学習時間の側で効果が
あった。

**3.6〜3.8 節の理論と観測の整合**:

- **3.6 節(演算ごとの精度の割り当て)**: MPS 上の実測として述べた内容が、T4 上でも
  すべて成り立った。
  - サブレイヤー出力は FP16 で計算される。
  - RMSNorm の出力と、残差接続後の Decoder Block の出力は FP32 になる。
  - logits は FP16、loss は FP32 になる(7.2 節)。
  - 加えて、6.13 節の単体呼び出しで、autocast 下の softmax の出力(Attention の
    確率行列)が FP32 であることを確認した。これは、3.6 節の「softmax は FP32 で
    行うことが推奨される」に沿った割り当てである。
- **3.7 節(gradient clipping との順序)**: 実験 F の判定・診断量ともに、導出と
  整合した。
- **3.8 節(メモリの内訳)**: パラメータ側の内訳(16 バイト / パラメータ)は一致した。
  一方、「削減の主因は活性化のメモリの FP16 化」という予測は、実験 G で反証された。
  この不一致については、次の段落の事後的な解釈で扱う。

**事後的な解釈(事前宣言した判定とは独立)**: 3.8 節の議論は、活性化が FP16 で
保持されれば要素あたり 4 バイトが 2 バイトになることを暗に前提としていた。7.9.1 節の
診断では、系列長の 2 乗で効く保存テンソルは逆に、要素あたり 4 バイト(FP32)から
6 バイト(FP32 の softmax 出力と FP16 の写しと解釈できる)に増えていた。3.6 節の
割り当て(softmax を FP32 に保つ)が、3.8 節の前提をこの項について崩した可能性がある。その他の保存テンソルは FP16 化によって約 35% 減っていた。
両者の比重が系列長とともに入れ替わることは、ピークメモリの差の符号が系列長 1024 で
正に転じたことと向きが一致する。ただし、これは結果を見た後に立てた解釈であり、次の
限界がある。

- ピークメモリの差の大きさ(208.0 MB)は、保存テンソルの差(約 980 MB)と一致しない。
- どの演算がどのテンソルを保存したかは直接確かめていない。

$\rho > 1$ の原因として検証されたものではない。また、本リポジトリの Attention の実装・
この PyTorch のバージョンに固有の結果でありうる。

**事後的な観察(判定基準を設けていない)**: 7.1 節の探索的検証では、初期化直後の
モデルで $S = 2^{18}$ から非有限な勾配が生じた。一方、実験 D の動的損失スケーリングは、
学習の後半に $S = 2^{21}$〜$2^{22}$ で非有限値をほとんど出さずに更新を続けた(スキップ
比率は最大 0.67%)。勾配の大きさの分布が学習の進行とともに変わることを示唆するが、
本ノートブックではこれを測定していない。静的損失スケールを初期化直後の探索に基づいて
決めることの妥当性は、検証の対象外である。

**関連するトピック**: 7.9.1 節の事後的な解釈で支配的だった項は、Attention の確率行列
(系列長の 2 乗の大きさ)を逆伝播のために保存することに由来する。014 で扱う
Flash Attention は、タイリングと online softmax によって確率行列を保持せずに逆伝播時に
再計算するため、この項の扱いそのものを変える。また、本トピックが浮動小数点の
ビット幅の削減を扱ったのに対し、013 では整数への量子化によって、さらに少ない
ビット数で重みを表す方法を扱う。


## 元ノートブック(実装の全文はこちら)

https://github.com/kojikojiprg/ai-theories/blob/main/theories/03_efficient_training/011_mixed_precision_training.ipynb
